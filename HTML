<!DOCTYPE html>
<html lang="zh-Hant">
<head>
  <meta charset="UTF-8">
  <title>糖霧森林</title>
  <style>
    body {
      background-color: #fdf6f0;
      font-family: '微軟正黑體', sans-serif;
      margin: 0;
      padding: 0;
    }
    .forest {
      background: linear-gradient(to bottom, #ffe0f0, #fffaf0);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
      overflow: hidden;
    }
    .tree {
      width: 60px;
      height: 100px;
      background: #d0f0c0;
      border-radius: 0 0 30px 30px;
      position: absolute;
      bottom: 0;
    }
    .tree::before {
      content: '';
      width: 0;
      height: 0;
      border-left: 30px solid transparent;
      border-right: 30px solid transparent;
      border-bottom: 40px solid #a0d0a0;
      position: absolute;
      top: -40px;
      left: 0;
    }
    .tree1 { left: 20%; }
    .tree2 { left: 40%; }
    .tree3 { left: 60%; }
    .tree4 { left: 80%; }
    .mist {
      position: absolute;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at 50% 50%, rgba(255, 255, 255, 0.5), transparent 70%);
      animation: float 6s ease-in-out infinite;
    }
    @keyframes float {
      0% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0); }
    }
    .title {
      position: absolute;
      top: 20%;
      width: 100%;
      text-align: center;
      font-size: 3em;
      color: #c080a0;
      text-shadow: 2px 2px 4px #fff;
    }
  </style>
</head>
<body>
  <div class="forest">
    <div class="mist"></div>
    <div class="title">糖霧森林</div>
    <div class="tree tree1"></div>
    <div class="tree tree2"></div>
    <div class="tree tree3"></div>
    <div class="tree tree4"></div>
  </div>
</body>
</html>
